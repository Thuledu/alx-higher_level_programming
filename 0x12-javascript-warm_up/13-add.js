#!/usr/bin/node
/**
 * Returns the addition of two integers
 * @a - The first integer
 * @b - The second integer
 * @returns - The sum of the two integers
 */
function add(a, b) {
	return a + b;
}

module.exports = { add };
